The `examples/` directory is a snapshot of a large subset of the `xs` code
that I run on all of my personal and work computers.

Note that the leading dot on example file and directory names has been
replaced with an underscore.
