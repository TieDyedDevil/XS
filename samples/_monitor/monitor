#! /usr/bin/env xs

ARG0 = $0
FROM = `{basename $ARG0}
HERE = `{cd `{dirname $ARG0}; pwd}
. $HERE/osd_pipe
if {!result <={pgrep -fxc '.*xs .*\.monitor/monitors/.*$' >/dev/null}} {
	for f $HERE/monitors/* {
		setsid $f >/dev/null >[2=1] &
	}
}
