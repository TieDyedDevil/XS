#! /usr/bin/env xs

if {~ `tty *tty1} {
	startwm
} else {
	stty start undef stop undef
	%is-login && {
		~ `tty /dev/tty* && vtfbfit
		.herald
	}
	if {!~ $history () && ~ `{wc -l $history} [4-9]???} {
		echo 'Consider histclean'
	}
	.xsin-rp
}
