#! /usr/bin/env xs

printf 'Kill panel processes. Use with caution. Proceed? '
let (fn-rcnl = {let (c = <=%read-char) {printf \n; result $c}}) {
	~ <=rcnl y || exit
}
pkill -f panel.xs
pkill tail
pkill mpc
pkill nmcli
pkill herbstclient
