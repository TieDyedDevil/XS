#! /usr/bin/env xs

#: write a message to all my logged-in tty and pty
#: and pop up a transient message via osd

ARG0 = $0
FROM = `{basename $ARG0}

. $HOME/.monitor/osd_pipe
. $HOME/.monitor/common

announce $*
